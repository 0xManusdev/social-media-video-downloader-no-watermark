# Only needed when installing yt-dlp via pip instead of the standalone binary.
# The curl-cffi extra is required: without browser impersonation, Instagram
# redirects every anonymous post request to its login page.
yt-dlp[default,curl-cffi]>=2025.1.1
